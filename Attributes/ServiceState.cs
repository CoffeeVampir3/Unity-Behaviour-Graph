namespace BehaviourGraph.Services
{
    /// <summary>
    /// Allows a service to signal if it is still running or if it has completed.
    /// </summary>
    public enum ServiceState
    {
        Running,
        Complete
    }
}